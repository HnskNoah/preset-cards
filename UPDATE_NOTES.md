# preset-cards 更新说明

> 状态：`feature/preset-migration` 合并前审查稿。迁移功能仍属于实验性功能，最终合并前必须完成完整验证。
## 已落地功能修复（非迁移）

- `saveMeta` 合并窗口现在按字段合并本体 patch，`extensions` 递归合并；保存窗口固定目标对象身份，删除、同名替换或数组索引漂移会显式失败，不会误报成功或写入新目标。
- 活动预设保存成功后完整同步 `oai_settings.extensions`，原生下一次保存不会再用旧运行时正则等扩展覆盖新内容。
- 父预设删除时注销投影会同步清理活动投影名和 `activeProfile`；插件删除路径先注销投影、随后发 `PRESET_DELETED` 也不会留下悬空活动指针。
- 扩展数组 toggle 路径按已知字段后缀解析，数组条目 id 含点号时可以完成捕获 → 应用闭环。
- 编辑器提交/重置的现有副本事务与真实预设槽位校验保持一致；保存失败不会污染运行时或持久 meta。

## 功能更新

### 预设配置迁移 v2

- 支持把旧预设上的整棵 Base/Delta profile 树迁移到更新后的预设。
- Prompt 条目优先按 `identifier` 匹配；identifier 变化但内容指纹唯一一致时自动重映射；重复指纹不会冒险自动连接。
- 使用逐层重放语义：
  - Base 层比较旧出厂值、用户修改和新版出厂值。
  - Delta 层比较旧父链解析值、本层修改和迁移后父链解析值。
- 上层冲突裁决可能使下层冲突新增或消失；冲突列表会随裁决重新计算。
- 冲突在独立迁移编辑器中解决，提供旧出厂值、新版值、我的修改和手动编辑四种选择。
- 未解决冲突时不能应用迁移；关闭编辑器不会落盘。
- 顺序策略支持“保留我的顺序”和“跟随新版顺序”；新条目支持跟随新版出厂挂载或保持未挂载。
- 迁移结果追加到目标预设，不替换目标已有 profiles；profile id 冲突时会重分配并同步改写 Delta 的 `baseId`。
- 默认不带入来源预设内容。开启“从来源预设带入”后，可带入迁移 profile 引用但目标缺失的 prompt 定义和来源正则；目标已有正则内容优先，重叠条目的开关状态按来源处理。
- 迁移入口打开时会提示该功能具有实验性，建议先导出预设备份并核对迁移结果。

### 采样参数

- `stream_openai`（流式输出）和 `show_thoughts`（请求思维链）均纳入 profile 的采样参数快照。
- 两个参数会沿 Base/Delta 父链继承，并参与保存捕获、注册投影、迁移重放和 reset。
- 编辑预设时，两个复选框位于采样参数区；当前布局为上排四项（T/P/K/请求思维链），下排三项（上下文/回复 Tokens/流式输出），两行各自占满宽度。
- 编辑当前激活预设后，采样改动会同步到 ST 运行时设置和对应面板控件。
- 升级前已经存入 `extra` 的 `show_thoughts` 会在解析时被采样参数通道接管，避免旧基线覆盖新采样值。

### 注册与捕获

- profile 可投影为 SillyTavern 原生预设，支持原生下拉切换。
- 原生 Prompt Manager 的字段、开关、顺序、增删和顶层参数变更会捕获回 profile。
- 扩展配置覆盖支持沿 profile 父链应用：数组条目可新增、摘除和覆盖；布尔开关采用后写者优先。
- 扩展捕获以“父预设加祖先 profile 覆盖后的继承状态”为基线，避免祖先层条目在后代 profile 中被错误记录或重新出现。

## 兼容性与注意事项

- profile 持久化位置仍是预设的 `extensions['preset_cards']`，没有引入独立数据文件。
- 迁移是追加式操作，旧预设不会自动删除；执行前仍建议导出来源和目标预设作为备份。
- 目标预设如果正处于未注册的字段级 profile 应用状态，不应直接作为迁移目标；请先切回稳定的预设状态后再迁移。
- 迁移期间如果来源或目标发生变化，应重新打开迁移流程，不能继续使用旧冲突裁决。
- 旧版预设缺少新设置键时，ST 的当前值仍可能作为缺省值；对非常旧且从未被新版 ST 保存过的预设，建议先在 ST 原生界面保存一次。

## 验证状态

- `npm run typecheck`：通过。
- 定向非迁移回归：6 个测试文件、64 个用例通过。
- `npm test`：29 个测试文件、249 个用例通过。
- 非迁移修复覆盖：持久化 patch/目标身份、活动投影删除与扩展回灌、扩展点号 id、reset 真实预设槽位行为。
- 迁移 v2 仍属于实验性开发功能；本轮没有修复迁移专属语义，也不因此通过 `dev-ts` 合并门。

## 迁移 v2 当前未决问题

以下仅属于迁移开发范围，不能作为已修复功能使用：

- 投影刷新门仍是模块级状态，跨 A/B 投影切换期间可能吞掉 B 的刷新。
- `undefined` 合法裁决与编辑器过期 resolution 尚未区分/校验。
- 孤立 Delta、Delta `order` 的 follow-new 语义尚未收敛。
- 锁定基线目标的 factory 状态、迁移正则无 id 匹配、sampling/extra/model 三方语义尚未完成。

> 本说明记录非迁移修复后的当前状态，不代表迁移 v2 已通过合并门。

